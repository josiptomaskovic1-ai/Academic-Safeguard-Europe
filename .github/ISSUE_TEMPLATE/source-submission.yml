name: Source submission
description: Suggest an official document or a background reference.
title: "[Source] "
labels: [source]
body:
  - type: markdown
    attributes:
      value: |
        **This issue is public.** Do not include personal data, names of staff or students, or allegations about individuals. Only submit publicly available documents.
  - type: dropdown
    id: kind
    attributes:
      label: Kind of source
      options:
        - Primary source for scoring (law, statute, regulation, procedure)
        - Evidence Library item (report, standard, research)
    validations:
      required: true
  - type: input
    id: title
    attributes:
      label: Title (original language)
    validations:
      required: true
  - type: input
    id: body
    attributes:
      label: Issuing body or authors
    validations:
      required: true
  - type: input
    id: url
    attributes:
      label: URL
    validations:
      required: true
  - type: input
    id: jurisdiction
    attributes:
      label: Jurisdiction (ISO code, EU, EHEA or International)
    validations:
      required: true
  - type: input
    id: ref
    attributes:
      label: Official reference or publication date (if known)
  - type: textarea
    id: relevance
    attributes:
      label: Why is it relevant? Which safeguards does it concern?
    validations:
      required: true
  - type: checkboxes
    id: confirm
    attributes:
      label: Confirmation
      options:
        - label: This submission contains no personal data and no allegations about individuals.
          required: true
